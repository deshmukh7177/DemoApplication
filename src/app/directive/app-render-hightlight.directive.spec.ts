import { AppRenderHightlightDirective } from './app-render-hightlight.directive';

describe('AppRenderHightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AppRenderHightlightDirective();
    expect(directive).toBeTruthy();
  });
});
